# FASE 2.5: Blueprint de Implementação (Pré-Fase 3)

Como Arquiteto-Estrategista, **NUNCA codifico lógica de produção**. No entanto, o detalhamento do Schema SQL (DDL), da Estrutura de Diretórios e do Plano de Sprint são **especificações arquiteturais obrigatórias** para garantir que a implementação (Fase 3) siga rigorosamente os mandamentos de Core Isolado, Zero-Trust e Event Sourcing.

Abaixo está o guia definitivo para a execução.

---

## 1. 🗂️ ESTRUTURA DE DIRETÓRIOS (Dual Deploy Ready)

```text
devcod/
├── .github/
│   └── workflows/                # CI/CD (Testes, Lint, Coverage, Score Metrics)
├── src/
│   ├── core/                     # 🚨 NÚCLEO ISOLADO (Zero dependências de framework)
│   │   ├── score/                # ScoreCalculator v1 (Cyclomatic, Domain Files, Assertions)
│   │   ├── challenge/            # ChallengeValidator (Regras de pesos mínimos)
│   │   └── dispute/              # DisputeResolver (Lógica de votação do comitê)
│   ├── adapters/                 # PORTAS E ADAPTADORES
│   │   ├── vcs/                  # VCSAdapter v1 (Implementação GitHub, interface para futuro GitLab)
│   │   ├── payment/              # PaymentAdapter v1 (Stripe Connect)
│   │   └── db/                   # DataAdapter v1 (Supabase client wrapper)
│   └── web/                      # ADAPTADOR WEB (Next.js 14 App Router)
│       ├── app/
│       │   ├── (marketing)/      # Landing page, waitlist
│       │   ├── (dashboard)/      # Painéis (Dev, Maintainer, Admin)
│       │   └── api/              # API Routes (Webhooks com validação HMAC, Auth)
│       └── middleware.ts         # Validação de JWT e Roles em todas as rotas protegidas
├── supabase/
│   ├── migrations/               # Schema SQL e RLS policies (versionadas)
│   ├── functions/                # Edge Functions (award-devcoins, verify-event-log, resolve-dispute)
│   └── seeds/                    # Dados de teste para E2E
├── tests/
│   ├── unit/                     # Jest (Core puro)
│   ├── integration/              # Jest + nock/stripe-mock (Adaptadores)
│   └── e2e/                      # Playwright (Fluxos completos)
├── docs/                         # ADRs, Specs, API Contracts
├── package.json
└── README.md